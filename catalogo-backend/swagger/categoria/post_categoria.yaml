Crea una nueva categoría
---
tags:
  - "categoria"
responses:
  201:
    description: Se creó correctamente la categoría
  404:
    description: No se encontró la categoría
  500:
    description: Error inesperado al crear la categoría
parameters:
  - name: body
    in: body
    schema:
      $ref: "#/definitions/Categoriapost"
definitions:
  Categoriapost:
    type: "object"
    properties:
      descripcion:
        type: "string"
    example:
      descripcion: "Descripción de ejemplo para la categoría"
