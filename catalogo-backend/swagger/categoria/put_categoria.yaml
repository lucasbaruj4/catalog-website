Actualiza una categoría existente
---
tags:
  - "categoria"
responses:
  200:
    description: Se actualizó correctamente la categoría
  404:
    description: No se encontró la categoría
  500:
    description: Error inesperado al actualizar la categoría
parameters:
  - name: id
    in: path
    required: true
  - name: body
    in: body
    schema:
      $ref: "#/definitions/Categoriaput"
definitions:
  Categoriaput:
    type: "object"
    properties:
      descripcion:
        type: "string"
    example:
      descripcion: "Descripción de ejemplo para la categoría"
